/** Photo slots for مِسك. Files live in public/photos/misk/. */

import type { SlotMap } from "../shared/photo";

export const SLOTS: SlotMap = {
  hero: { label: "الصالة في ضوء النهار", ratio: 16 / 9 },
  cappuccino: { label: "كابتشينو كلاسيك", ratio: 1 },
  cheesecake: { label: "تشيز كيك نيويورك", ratio: 1 },
};

/** Regenerated by scripts/sync_photos.py. Empty means nothing uploaded yet. */
export const MANIFEST: Record<string, string> = {};
